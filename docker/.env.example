# lex-pdftotext Docker Configuration
# Copy this file to .env and customize as needed

# API Configuration
API_PORT=8000
MAX_UPLOAD_SIZE=52428800  # 50MB in bytes
LOG_LEVEL=INFO

# Worker Configuration
WORKER_REPLICAS=2

# Storage Configuration
# Options: local, s3
STORAGE_TYPE=local

# S3 Configuration (only if STORAGE_TYPE=s3)
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_ENDPOINT=  # Leave empty for AWS, set for MinIO/other S3-compatible
S3_REGION=us-east-1

# SaaS Configuration
# Set to true to enable API key authentication, rate limiting, and quotas
SAAS_MODE=false

# Database (only if SAAS_MODE=true)
# SQLite: sqlite:///./lex_pdftotext.db
# PostgreSQL: postgresql://user:pass@host:5432/dbname
DATABASE_URL=sqlite:///./lex_pdftotext.db
